.DS_Store
*.pyc
__pycache__/
.idea/
.vscode/

# Local secrets must never be committed.
.env
.env.*
.dev.vars
.dev.vars.local

# Syncthing leaves these when two devices edit the same file.
# They must never be staged — the content is older than whatever
# is already on disk.
*.sync-conflict-*

# Claude Code session state
.claude/
